# Local (docker-compose): postgresql://postgres:postgres@localhost:5432/file_explorer
# Produção (Railway): copiar DATABASE_URL do serviço PostgreSQL no Railway
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/file_explorer"

PORT=3001

# CORS (produção): URLs permitidas separadas por vírgula
# CORS_ORIGIN="https://seu-app.vercel.app"
